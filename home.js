let slideIndex = 0;
showSlides();

function showSlides() {
  let i;
  let slides = document.getElementsByClassName("mySlides");
  let dots = document.getElementsByClassName("dot");
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";  
  }
  slideIndex++;
  if (slideIndex > slides.length) {slideIndex = 1}    
  for (i = 0; i < dots.length; i++) {
    dots[i].className = dots[i].className.replace(" active", "");
  }
  slides[slideIndex-1].style.display = "block";  
  dots[slideIndex-1].className += " active";
  setTimeout(showSlides, 2000); // Change image every 2 seconds
}


  //Get the button
var mybutton = document.getElementById("myBtn");

// When the user scrolls down 20px from the top of the document, show the button
window.onscroll = function() {scrollFunction()};

function scrollFunction() {
  if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
    mybutton.style.display = "block";
  } else {
    mybutton.style.display = "none";
  }
}

// When the user clicks on the button, scroll to the top of the document
function topFunction() {
  document.body.scrollTop = 0;
  document.documentElement.scrollTop = 0;
}



function msaad614() {
  var elem = document.getElementById("myMsaad");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 85) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function nouf606() {
  var elem = document.getElementById("myNouf");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 99) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function abdulk608() {
  var elem = document.getElementById("myAbdulk");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 100) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function jackr602() {
  var elem = document.getElementById("myJackr");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 70) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function jackm607() {
  var elem = document.getElementById("myJackm");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 93) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function mohj603() {
  var elem = document.getElementById("myMouj");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 99) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function leon612() {
  var elem = document.getElementById("myLeon");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 75) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function since613() {
  var elem = document.getElementById("mySince");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 100) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function mubarak616() {
  var elem = document.getElementById("myMubarak");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 0) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function hamid615() {
  var elem = document.getElementById("myHamid");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 0) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}
function abdoy617() {
  var elem = document.getElementById("myAbdoy");   
  var width = 0;
  var id = setInterval(frame, 25);
  function frame() {
    if (width >= 0) {
      clearInterval(id);
    } else {
      width++; 
      elem.style.width = width + '%'; 
      elem.innerHTML = width * 1  + '%';
    }
  }
}